<Window x:Class="IndustrialTestPlatform.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Industrial Test Platform" Height="600" Width="900">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <Button Content="Start" Width="120" Margin="0,0,8,0" Command="{Binding StartCommand}" IsEnabled="{Binding CanStart}" />
            <Button Content="Stop" Width="120" Command="{Binding StopCommand}" IsEnabled="{Binding CanStop}" />
            <TextBlock Text="Run Status:" Margin="16,0,8,0" VerticalAlignment="Center" />
            <TextBlock Text="{Binding RunStatus}" VerticalAlignment="Center" FontWeight="Bold" />
        </StackPanel>

        <GroupBox Header="Slots" Grid.Row="1" Margin="0,0,0,12">
            <ItemsControl ItemsSource="{Binding Slots}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="12" Margin="6">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" />
                                <TextBlock Text="{Binding Status}" FontSize="13" Margin="0,6,0,0" />
                                <TextBlock Text="{Binding LastMessage}" TextWrapping="Wrap" Foreground="Gray" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </GroupBox>

        <GroupBox Header="Logs" Grid.Row="2">
            <ListBox ItemsSource="{Binding Logs}" FontFamily="Consolas" />
        </GroupBox>
    </Grid>
</Window>
